# DEDUP - Core dependencies
# All dependencies are optional - the application works with just Python standard library

# Performance hashing (recommended)
# xxhash provides much faster hashing than MD5
xxhash>=3.0.0; platform_python_implementation=='CPython'

# Trash support (recommended)
# send2trash provides proper trash/recycle bin support
send2trash>=1.8.0

# Optional UI drag-and-drop support
tkinterdnd2>=0.4.2

# Optional: Faster cryptographic hashing
# blake3 is faster than SHA256 for large files
# blake3>=0.3.0

# Development dependencies (not required for use)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# mypy>=1.0.0
